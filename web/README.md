# Maze Lab Web Demo

This folder contains the optional browser demo generated from the design files.

Open `index.html` in a browser to run it.

The main coursework implementation remains the Python app in the project root.
The files in this folder are intentionally separate and do not modify the Python
maze model, pathfinding algorithms, CLI, or Tkinter GUI.
